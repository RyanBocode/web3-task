# 运行端口
PORT=8080
# gin 模式: debug/release/test
GIN_MODE=debug

# JWT 密钥（务必在生产环境修改）
JWT_SECRET=dev_secret_change_me
JWT_TTL_HOURS=24

# 数据库配置：
# DB_DRIVER 可选 sqlite 或 mysql
DB_DRIVER=sqlite
# SQLite 文件名（DB_DRIVER=sqlite 时有效）
SQLITE_PATH=blog.db

# MySQL 连接串（DB_DRIVER=mysql 时使用）
# 形如：user:pass@tcp(127.0.0.1:3306)/blog?charset=utf8mb4&parseTime=True&loc=Local
MYSQL_DSN=root:password@tcp(127.0.0.1:3306)/blog?charset=utf8mb4&parseTime=True&loc=Local